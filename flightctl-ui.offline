# Use the Nginx image you just imported
FROM docker.io/library/nginx:alpine

RUN rm -rf /usr/share/nginx/html/*

COPY apps/standalone/dist/ /usr/share/nginx/html/

# Expose port 80
EXPOSE 80

# Start Nginx
CMD ["nginx", "-g", "daemon off;"]
